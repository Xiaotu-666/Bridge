1.地图标点与实际不匹配,这个应该是得到的坐标在地图上显示偏移。
2.初始检查以及定期检查的各项子检查记录，比如初始检查的初始检查记录，检测项目结果，初始病害，初始检查报告以及定期检查的定期检查任务，定期检查记录，部件检查记录，定期病害，定期检查报告等等的表头翻译成中文
3.增加一个功能，在每一个桥梁的查看桥梁信息功能里，对于每一个桥梁的初始检查记录表以及桥梁定期检查表，就行分别汇总，把多次检查的结果汇总起来，更加直观的体现桥梁的信息
4.在工作台/dashboard中，对于admin中的启用用户，可以设计一个饼图，不同角色占据不同的饼区。对于在册的桥梁可以分为几个类，比如梁式桥、拱桥、刚架拱、钢混组合、斜拉桥、悬索桥，如果新增一种桥梁类型，那么在工作台中的桥梁的饼图中显示其它。检查任务的表应该分成初始检查和定期检查，并且设计一个需要定期检查的数据和已经完成的定期检查的数据的显示，为一个x轴为时间，纵坐标检查次数，有两条线，一个是已检查的线，另外一个是还未检查的线，在最下面为全系统近期任务。
5.在点击角色工作台标签时出现bug，会将地址返回至http://localhost:5173/403，应该不同的角色访问对应的工作台及返回http://localhost:5173/dashboard/。
6.http://localhost:5173/base/routes，在新增中或者编辑中，我希望把路线等级弄成选项，选择高速公路等多种不同的公路。在新增中使用高德地图指定某个点然后添加路线，指定点之后传给高德，高德将信息传回来显示路线名称，并且自动选择路线等级，并展示该路线（在地图上显示路线），在编辑中展示某个路线的地图上的路线，然后对于路线名称来说这是唯一的。
7.在http://localhost:5173/base/parts部位字典中出现了sort order，把这个改成中文。对于这个顺序来说是唯一值。而且不能编辑顺序，只能编辑部位名称。
8.在http://localhost:5173/base/components部件字典中active flag应该换成对应的桥梁类型。这样才能在桥型-部位-部件矩阵自动生成对应的桥梁类型-部位-部件-顺序
9.在http://localhost:5173/base/component-matrix中，可以使桥梁类型为主题生成专属的桥梁类型卡片，点击卡片后跳转对应的http://localhost:5173/base/component-matrix/*中，里面显示该桥梁的部位，点击某个部位后就能显示桥梁的部件信息，原有的不用改变，只需要将原有的关系表放在http://localhost:5173/base/component-matrix下方即可。
10.在http://localhost:5173/base/initial-matrix中，桥型-初检项目矩阵也可以参照第9点的思路进行改变。
11.http://localhost:5173/archive/bridges中桥梁基础信息不应该输入关键词筛选，可以使用选择加输入多个类型的值进行筛选。
12.在桥梁基本状况卡片中添加生成桥梁基本状况卡片文件，文件样式为PDF格式，能够自动生成类似项目要求中的一样
13.初始检查报告和定期检查报告无法正常生成报告，注意报告应该为PDF格式，能够自动生成类似项目要求中的一样，注意在生成桥梁定期检查时会分成不同的桥梁表，分为梁式桥、拱桥、刚架拱、钢混组合、斜拉桥、悬索桥等桥梁，对梁式桥来说为表C-1，后面依次为表C-x，在最右上方应该添加桥梁定期检查记录表的专属编号。
14.在对上传图片的时候，在上面的图片不应该只为最近上传图片，可以进行切换并在该图片的框架的最下方用比较隐藏的字体写对应的图片信息，所以下方的上传图片需要修改为，多个图片的框架，有多个点击上传桥梁xx图片的按钮。
15.对于A桥梁所处行政区划 B行政识别数据 C桥梁技术指标需要美化。对于D桥梁结构信息，需要分成不同的部分要分成桥梁分孔、结构体系、上部结构形式与材料、桥面系形式与材料、下部结构形式与材料、基础形式与材料、支座形式与材料与附属设施。下面的是需要注意的：桥梁分孔需要根据孔数设置列数，结构体系根据种类设置列数，斜拉索（含索力）根据索数设置列数，吊杆（含索力）根据吊杆数设置列数，系杆（含索力）根据系杆数设置列数，伸缩缝根据孔数设置列数，栏杆、护栏根据部位不同设置列数，桥台根据桥台数设置列数，桥墩根据桥墩设置列数，锚碇根据锚碇数设置列数。
16.在进行修改桥梁基础状况时，我认为可以按照桥梁基本状况卡片，分成不同的行列形式类似与excel表的形式进行修改。